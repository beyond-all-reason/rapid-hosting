---
argument_specs:
  main:
    options:
      domain_name:
        required: true
        type: 'str'
        description: 'Full qialified domain name to configure'
      serving_root:
        required: true
        type: 'str'
        description: 'Path to the root of http server'
